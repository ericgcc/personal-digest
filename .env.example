DEEPSEEK_API_KEY=
# WOPS writing-operations library (external project, not part of this repo).
# Required for v2 writing-operation retrieval; without it developmental review still runs,
# the vocabulary falls back to the built-in copy, and revision gets no operations.
WOPS_ROOT=
OPEN_ROUTER_API_KEY=